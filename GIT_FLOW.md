# คู่มือการจัดการโค้ดด้วย Git Flow (Best Practice)

โปรเจกต์นี้ใช้โครงสร้าง **Git Flow** เพื่อแยกแยะการทดสอบ การพัฒนาระบบ และโค้ดสำหรับโปรดักชันออกจากกันอย่างชัดเจน เพื่อลดความเสี่ยงที่ระบบจะพัง

## 🌿 กิ่งหลัก (Main Branches)

โปรเจกต์นี้จะมีกิ่งหลักๆ ที่ไม่มีวันถูกลบทิ้ง 2 กิ่งคือ:
1. **`main` (หรือ `master`)**:
   - เป็นกิ่งสำหรับ **Production** เท่านั้น!
   - โค้ดในกิ่งนี้ต้องพร้อมใช้งาน 100% ห้าม Commit โค้ดที่ยังไม่เสร็จสมบูรณ์ลงกิ่งนี้เด็ดขาด
   - การนำโค้ดเข้า `main` ควรทำผ่าน Pull Request (PR) หรือ Merge จากกิ่ง `develop` เท่านั้น

2. **`develop`**:
   - เป็นกิ่งสำหรับ **Staging / Testing**
   - เป็นศูนย์รวมโค้ดใหม่ๆ ทั้งหมด ฟีเจอร์ที่เพิ่งเขียนเสร็จจะต้องถูกส่งมารวมกันที่นี่เพื่อทดสอบร่วมกันก่อนขึ้น `main`

## 🌱 กิ่งย่อย (Supporting Branches)

เวลาโปรแกรมเมอร์จะเขียนโค้ดใหม่ **ห้ามเขียนลง `main` หรือ `develop` ตรงๆ** ให้แตกกิ่งย่อยออกมาดังนี้:

### 1. กิ่งสร้างฟีเจอร์ใหม่ (`feature/*`)
- **แตกกิ่งออกมาจาก:** `develop`
- **เมื่อเสร็จแล้วต้อง Merge กลับไปที่:** `develop`
- **การตั้งชื่อ:** `feature/ชื่อฟีเจอร์` เช่น `feature/ai-pipeline`, `feature/external-api`

### 2. กิ่งแก้ไขบั๊กฉุกเฉินบน Production (`hotfix/*`)
- **แตกกิ่งออกมาจาก:** `main` (เพราะบั๊กเกิดบน Production จึงต้องดึงโค้ดปัจจุบันมาแก้)
- **เมื่อเสร็จแล้วต้อง Merge กลับไปที่:** `main` **และ** `develop` (เพื่อไม่ให้บั๊กเดิมกลับมาอีกในอนาคต)
- **การตั้งชื่อ:** `hotfix/ชื่อบั๊ก` เช่น `hotfix/deduplication-crash`

### 3. กิ่งเตรียมปล่อยเวอร์ชันใหม่ (`release/*`)
- **แตกกิ่งออกมาจาก:** `develop`
- **เมื่อเสร็จแล้วต้อง Merge กลับไปที่:** `main` และ `develop`
- **การตั้งชื่อ:** `release/vX.X.X` เช่น `release/v1.2.0`

---

## 📝 กฎการ Commit โค้ด (Conventional Commits)
เพื่อให้อ่านประวัติการทำงานได้ง่าย โปรดตั้งชื่อ Commit แบบมี Prefix เสมอ:
- `feat: [คำอธิบาย]` - สำหรับฟีเจอร์ใหม่
- `fix: [คำอธิบาย]` - สำหรับการแก้บั๊ก
- `docs: [คำอธิบาย]` - สำหรับอัปเดตเอกสาร (เช่น README, คู่มือ)
- `style: [คำอธิบาย]` - สำหรับจัดฟอร์แมตโค้ด, CSS ที่ไม่กระทบ Logic
- `refactor: [คำอธิบาย]` - สำหรับการจัดโครงสร้างโค้ดใหม่ แต่ผลลัพธ์เหมือนเดิม
- `perf: [คำอธิบาย]` - สำหรับการปรับปรุงประสิทธิภาพ (ให้โค้ดทำงานเร็วขึ้น/กินทรัพยากรน้อยลง)

**ตัวอย่างที่ดี:** 
`feat: add external api endpoint for audio analysis`
`fix: correct deduplication matching logic`
